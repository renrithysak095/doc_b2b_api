spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/renrithysak095/b2b_doc_config.git
          clone-on-start: true
          search-paths:
            - user-service
            - auth-service
            - help-service
            - doc-service
            - gateway-service
            - mainTitle-service
          default-label: master
encrypt:
  key: sdafghfdeweqads#sazdf1212
server:
  port: 8888